// для взаимодействия с пользователем:
void waitForEnter();
char choice(char *);

// для преобразования строк:
char *toRU(const char *);  // из ANSI в ASCII
char *intToStr(int);
char *lowerCase(char *);

bool inOrder(const int, const int,const int);
